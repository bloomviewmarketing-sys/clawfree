-- Sample data for development
-- Note: In production, profiles are created via Supabase Auth triggers

-- This seed file is meant to be run after setting up a test user through Supabase Auth
-- Replace the UUID below with your actual auth.users id

-- Example: insert a skill
-- insert into user_skills (user_id, name, description, triggers, instructions)
-- values (
--   'your-user-id-here',
--   'summarize-url',
--   'Summarize the content of any URL',
--   array['summarize this url', 'tldr this page', 'summarize'],
--   'Fetch the given URL, extract the main content, and provide a concise summary with key points.'
-- );
